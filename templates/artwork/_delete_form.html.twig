<form method="post" action="{{ path('app_artwork_delete', {'id_artwork': artwork.idartwork}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ artwork.idartwork) }}">
    <button class="btn">Delete</button>
</form>
