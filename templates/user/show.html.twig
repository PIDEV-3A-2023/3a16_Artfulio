{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}

<section class="gallery2 cid-tueENDWKwP" >
<div class="container">
<div class="row mt-4">
    <table class="show" >

    <h1>User</h1>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Cin_user</th>
                <td>{{ user.cinUser }}</td>
            </tr>
            <tr>
                <th>Adresse_user</th>
                <td>{{ user.adresseUser }}</td>
            </tr>
            <tr>
                <th>Password_user</th>
                <td>{{ user.passwordUser }}</td>
            </tr>
            <tr>
                <th>Email_user</th>
                <td>{{ user.emailUser }}</td>
            </tr>
            <tr>
                <th>Is_pro</th>
                <td>{{ user.isPro ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Img_user</th>
                <img style="width : auto;height:auto;" src="{{ asset('uploads/images/' ~ user.imgUser) }}"  />                

                <td>{{ user.imgUser }}</td>
            </tr>
        </tbody>
    </table>
</div>

    <a href="{{ path('app_user_index') }}">back to list</a>

    <a href="{{ path('app_user_edit', {'id': user.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
    </div>
    </section>

{% endblock %}
