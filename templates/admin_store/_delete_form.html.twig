<form method="post" action="{{ path('app_admin_store_delete', {'id_produit': store.idProduit}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ store.idProduit) }}">
    <button class="btn">Delete</button>
</form>
