<form method="post" action="{{ path('app_collaboration_delete', {'idCollaboration': collaboration.idCollaboration}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collaboration.idCollaboration) }}">
    <button class="btn">Supprimer</button>
</form>
